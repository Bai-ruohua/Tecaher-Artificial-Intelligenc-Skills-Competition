# V4.1 升级计划：课程切换联动 + 布局修复 + 首页学情同步

> 文档版本：V4.1 | 日期：2026-07-23 | 负责人：朱景峰
> 项目目录：`Tecaher-Artificial-Intelligenc-Skills-Competition\`
> 基线快照：`releases/v4.0/`

## 变更清单

### 1. 课程切换全联动（P2-5）
- 新增 `templates/teacher/_course_bar.html` 统一课程选择栏组件
- 所有教师页面（含 internship、training_docs）统一添加课程选择器
- `app.py` 中 `page_internship`、`page_training_docs` 补全 `_course_ctx()` 调用
- 切换课程后 `location.reload()` 所有页面同步切换

### 2. 首页布局优化
- 9 个指标合并为一行（减负大盘 4 + 学情指标 5）
- 去除首页 KPI 行与学情看板 KPI ���的数据重复
- `kpi-row` CSS 样式移除，统一使用 `metric-row`

### 3. 学情看板升级
- Chart.js 替换为 ECharts 5.5（与 analytics 页一致）
- 新增综合分分布甜甜圈图
- 排名表替换为含金银铜徽章 + 彩色进度条的高级版本

### 4. 章节选择修复
- `ch.id` → `ch.chapter_id`（影响 4 个模板 6 处选项）

## 涉及文件
- `src/app.py`：2 条路由补 `_course_ctx()`
- `src/config.py`：`PLATFORM_VERSION = "4.1"`
- `CHANGELOG.md`：新增 [4.1] 条目
- `templates/teacher/_course_bar.html`：新增课程栏组件
- `templates/teacher/dashboard.html`：指标合并 + ECharts 替换
- `templates/teacher/internship.html`：添加课程栏
- `templates/teacher/training_docs.html`：添加课程栏
- `templates/teacher/lesson_prep.html`：`ch.id` 修复
- `templates/teacher/exam.html`：`ch.id` 修复（2处）
- `templates/student/agent_quiz.html`：`ch.id` 修复
- `templates/student/agent_digital.html`：`ch.id` 修复
- `static/css/style.css`：metric-row 样式调整 + chart-card/dashboard-grid 样式
- `static/css/func.css`：排名表 + bar-wrap 样式
