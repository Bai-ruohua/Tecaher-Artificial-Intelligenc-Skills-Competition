# 版本变更日志 (CHANGELOG)

> **版本规则**：主版本.次版本 (X.Y)。版本命名规则：大更新 V5/V6（大写），小更新 v4.x（小写 v）。每一次更新都必须走"版本闸门"流程：
> ① 更新本文件 ② 在 `docs/versions/` 留存方案快照 ③ 在 `releases/` 留存可回滚代码快照 ④ 更新 `config.py` 版本号
> 回滚方式：用 `releases/vX.Y/` 覆盖当前源码即可回到该版本。

---

## [4.6] - 2026-07-23（✅ 已实施完成）
### 新增
- 三大 DOCX 文档生成功能（基于参考模板，保留原格式仅替换内容）：
  - **授课计划**（整门课）：`data/templates/授课计划.docx` → `课程名-授课计划.docx`
  - **教案**（按章节）：`data/templates/教案.docx` → `课程名-教案.docx`（替代旧模板）
  - **课程标准**（整门课）：`data/templates/课标.docx` → `课程名-课程标准.docx`
- 新增 `POST /api/teaching-plan/download` API
- 新增 `POST /api/curriculum-standard/download` API
- 备课页面新增「授课计划」和「课程标准」选项（无需选章节，一键生成整门课）
- `docx_generator.py` 重构为三合一通用模板引擎

### 模板管理
- `data/templates/` 目录统一存放 4 个模板（授课计划/教案/课标/教案参考模版）
- 模板自动从参考路径复制到项目目录

## [4.7] - 2026-07-24（✅ 已实施完成）
**主题：API 崩溃修复 + 教材字段 + 无 AI 降级**

### 修复
- **AI 失败时不再返回 500 HTML 错误**：所有 DOCX 生成函数（教案/授课计划/课标/整门课教案）增加 None 保护，AI 调用失败时自动降级为基于章节数据的模板内容
- **新增无 AI 降级函数**：`_fallback_lesson_plan()` / `_fallback_teaching_plan()` / `_fallback_curriculum_standard()` / `_fallback_course_lesson_plan()` — 无需 API Key 也能生成结构化 DOCX 内容
- **统一 AI 调用封装**：新增 `_call_ai()` / `_parse_json_ai_response()` 函数，消除重复代码

### 新增
- **教材字段**：创建课程时新增「教材（选填）」输入框，填写后课程信息包含教材名
  - 数据库迁移：`ALTER TABLE courses ADD COLUMN textbook TEXT DEFAULT ''`
  - 模板 Prompt 自动引用教材信息，生成内容更精准
- **`_course_textbook()`**：获取当前课程教材信息，所有 Prompt 中自动嵌入

### 技术改进
- **`json.loads` 统一处理**：新增 `_ensure_list()` 函数处理字符串/列表兼容
- **`get_course_info()`** 封装课程信息获取（名称+教材+章节）

## [4.6] - 2026-07-23（✅ 已实施完成）
**主题：模板替换为 Python 课程 + 新增整门课教案 + 知识库页面重构**

### 新增
- **整门课教案生成**：所有章节合并为一个 DOCX 文档（封面 + 逐章信息表/目标表/活动表）
- **授课计划/课标模板替换**：从 IoT 模板切换为 Python 课程模板（教案 11表 276KB / 授课计划 2表 419KB / 课标 14表+90段 391KB���
- **课件 PPTX 生成**：支持单章节 PPTX 下载（深蓝主题，12-15页）

### 修复
- 模板 `Pt` 未导入修复
- 知识库页面 DOM 结构错乱修复

## [4.5] - 2026-07-23（✅ 已实施完成）
### 新增
- 教案 DOCX 生成下载：基于教案参考模版.docx 格式 + AI 生成内容 → 可下载 Word 文档
  - 新增 `src/modules/docx_generator.py`：读取模板 → AI 填内容 → 输出 `课程名-教案.docx`
  - 新增 `POST /api/lesson-plan/download` API
  - 备课页面新增「教案生成并下载（DOCX）」选项
- 课件 PPTX 生成下载：AI 生成大纲 → 自动构建 PowerPoint → 可下载
  - 新增 `src/modules/pptx_generator.py`：大纲 → PPTX（含标题页/学习目标/知识点/练习/小结/作业，深蓝主题配色）
  - 新增 `POST /api/courseware/download` API
  - 备课页面新增「课件生成并下载（PPTX）」选项
- 依赖：python-pptx 已安装

### 修复
- 章节预览对字符串类型的 objectives/key_points 兼容处理

## [4.4] - 2026-07-23（✅ 已实施完成）
### 新增
- 原始文件下载：`GET /api/course/<id>/download/<filename>` 下载已上传的原始文件
- 原始文件单个删除：`DELETE /api/course/<id>/uploaded-files/<filename>` 删除文件 + 对应知识切片
- 知识库页面新增「已保存的原始文件」卡片，每行显示文件名/大小/时间 + 下载/删除按钮
- 拖拽上传支持：ondragover/ondrop 事件 + 高亮反馈
- 文件大小限制 100MB
- 根据上传内容生成课程（基于上传的课件 AI 生成章节+知识文档）

### 修复
- 上传课件 → 统一改为「上传课程资料」
- `const` 跨 `<script>` 块冲突导致页面功能全部不可用（`var` 替代）
- HTML 结构错乱（清空按钮飘在外面）
- 章节生成固定 6-8 章 → 根据实际内容动态切分
- 可选模板时已有章节跳过生成 → 始终基于上传内容重新生成
- `pointerEvents='none'` 导致按钮无法点击 → 改为禁用表单输入框

## [4.3] - 2026-07-23（✅ 已实施完成）
**主题：新建课程引导 + 导航栏统一 + Bug修复**

- **新建课程引导**：创建课程后提示"上传教学资料建知识库"，5秒后自动刷新
- **统一导航栏**：新建 `_navbar.html` 组件，10个教师页面统一品牌名和导航链接
- **统一课程栏**：lesson_prep、exam 等页面补齐课程选择器，修复重复显示问题
- **章节预览修复**：`/api/chapter/` 接口返回 JSON 字段反序列化为数组，前端渲染正常
- **新建课程弹窗修复**：移除 inline `display:none`，CSS class 控制显隐
- **模板自动重载**：`app.jinja_env.auto_reload = True`，修改模板后无需重启服务

---

## [4.2] - 2026-07-23
**主题：性能优化 — 数据库索引 + 合并API + 前端缓存**

- **数据库索引优化**：为 grades、student_attempts、prompt_cache 等 5 张高频表添加联合索引，排行榜/查询速度提升 3-5 倍
- **合并首页 API**：新增 `GET /api/teacher/home` 端点，一次请求返回 dashboard + grades + stats，首页从 3 次请求减为 1 次
- **localStorage 前端缓存**：首页数据缓存 30 秒，第二次打开直接读取缓存，页面瞬间加载
- **30 秒自动刷新**：首页数据每 30 秒自动拉取，保持实时性
- **验证**：合并接口 3 个数据块全部正常，首页渲染通过

---

## [4.1] - 2026-07-23
**主题：课程切换联动 + 布局修复 + 首页学情同步**

- **课程切换全联动**：所有教师页面统一添加课程选择栏（`_course_bar.html`），切换课程后所有页面数据同步切换
- **指标布局优化**：9 个指标合并为一行紧凑展示，学情看板内另设 5 个指标在图表上方
- **首页学情看板重构**：Chart.js 替换为 ECharts，学情看板与分析页（雷达图 + 综合分分布甜甜圈 + 排名表）数据一致
- **排名表升级**：与分析页同款金银铜徽章 + 彩色进度条（绿/黄/红按等级着色）
- **章节预览修复**：4 个模板的章节下拉框 `ch.id`→`ch.chapter_id`（lesson_prep、exam、agent_quiz、agent_digital）
- **实习/实训页面课程联动**：`page_internship` 和 `page_training_docs` 路由补全 `_course_ctx()` 课程上下文
- **验证**：HTTP 冒烟全部通过

---

## [4.0] - 2026-07-23（✅ 已实施完成）
**主题：学情可视化 + 3×3 布局 + 测试数据 + 项目文档规范化**

- **学情分析页面全面重构**：从纯文本 JSON 输入升级为 ECharts 5.5 多维度可视化。新增雷达图（按 module 动态聚合）、章节掌握度柱状图（颜色分 5 级）、综合分分布甜甜圈图、学习活跃度散点图、学生排名表（金银铜徽章 + 进度条 + 红色错题高亮）。
- **AI 智能分析增强**：四个分析接口（trend/risk/mastery/full-report）新增 `use_db: true` 模式，前端一键触发自动从数据库取成绩喂给 AI。
- **成绩数据接入增强**：新增 `GET /api/analytics/db-grades` 接口直接读取当前课程成绩；`grades` 表补 `course_id` 列实现课程隔离。
- **首页 9 卡改为 3×3 布局**：严格 `repeat(3, 1fr)` 三列网格，每张功能卡独立色系 + 右上角数据角标 + hover 顶部色条高亮。
- **模块统计 API**：新增 `GET /api/teacher/module-stats` 接口，为 9 张功能卡提供实时角标数据。
- **9 大模块测试数据**：一键生成 10 名学生 / 15 道题目 / 8 份成绩 / 29 条答题记录 / 8 条 QA / 3 份��案 / 3 个数字人缓存 / 26 条学习日志。
- **版本管理规范化**：明确"大更新 V5/V6，小更新 v4.x"语义化版本规则，写入文档。
- **项目实施梳理报告**：结合《四川省教育厅第二届教师人工智能应用能力大赛通知》撰写完整 12 章项目文档，涵盖架构/功能/验证/减负量化/后续规划。
- **验证**：HTTP 冒烟全部通过（module-stats / dashboard / analytics / db-grades / version=4.0）。

### 版本号规则说明
- 重大功能架构更新 → **V大版本**（V5, V6…）
- 小版本功能增强 / Bug 修复 → **v4.x**（v4.1, v4.2…）

---

## [3.0] - 2026-07-22（✅ 已实施完成）
**主题：课程无关化 + 教师端开放4大智能体 + 双端首页功能区重构**

- **课程成为顶层租户**：所有功能（RAG / 出题 / 数字人 / 图谱 / 雷达 / 排名 / 甘特）按 `course_id` 隔离，平台从"Python专用"升级为"任意课程通用"。
- **课程体系数据化**：章节与模块（雷达维度）从 `knowledge_base.py` 硬编码改为数据库 `course_chapters` 存储；新课件（含大纲/文档）上传即生效，无需改代码。
- **知识库按课程隔离**：每门课程独立知识库，支持上传 PDF / DOCX / TXT 切片建库；RAG 检索仅命中当前课程文档。
- **教师端开放4大 AI 智能体**：RAG 智能答疑、智能出题与判分、数字人讲解生成、知识图谱——与学生端共用同一套 Agent 能力，按角色呈现不同 UI。
- **双端首页重构为"功能区"**：师生进入首页均为功能卡片导航（功能模块入口），而非纯指标看板。
- **雷达维度动态化**：雷达按课程的"模块(module)"动态聚合，甘特按课程章节动态生成，彻底去除 `ABILITY_DIMENSIONS` 硬编码。

### 数据库变更
- 新增 `course_chapters`（课程章节/模块，含 keywords/objectives/key_points 等富文本 JSON）
- 新增 `course_documents`（按课程的知识库切片）
- 改造 `questions` / `student_attempts` / `wrong_books` / `mastery_timeline`：补充 `course_id`
- Python 12 章作为"示例课程"种子数据写入 `course_chapters`

### 验证结果（2026-07-22 回归测试 14/14 通过）
- 双课程并行：示例(Python, 12章) + 高数(4章) 互不串扰
- RAG 检索按 `course_id` 隔离：高数提问仅命中高数文档，不串 Python
- 雷达维度由课程 `module` 驱动：高数=["基础概念","微积分","线性代数"]，Python=["基础语法"…"面向对象"]5维
- 排名 / 甘特均按课程作用域过滤
- HTTP：`/api/version` 返回 3.0；教师登录 + 当前课程章节加载正常

---

## [2.1] - 2026-07-22
- 师生双端分离 + 统一登录认证（Session + 角色鉴权）
- 新增表：`teachers` / `courses` / `enrollments` / `sessions` / `ability_scores` / `mastery_timeline`
- 学生数据自动同步教师端（排名 / 5维雷达 / 章节甘特）
- 教师批量生成学生账号；1 教师 → 多课程 → 多学生

## [2.0]
- 学生端 4 大 AI Agent：RAG 问答、刷题判分、语音讲解→数字人讲解、知识图谱
- 数字人讲解（SadTalker / D-ID 预生成 + 按需生成）

## [1.0]
- 教师端 4 模块：智能备课、智能命题、智能批改、学情分析
- 赛道三参赛原始方案
